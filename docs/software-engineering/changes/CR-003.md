# Change Request CR-003

## Request

Remove the unapproved Step 6 architecture draft and keep the repository lifecycle scope at Step 1–5 for now.

## Metadata

| Item | Nilai |
|---|---|
| Requester | Stieve - Project owner/stakeholder proxy |
| Request date | 1 Juli 2026 |
| Type | Removal / scope sequencing |
| Urgency | Normal |
| Decision | Approved and applied |
| Approval evidence | User statement: “yang architektur design hapus dlu deh soalnya saya hanya mau sampe step 5 aja dlu” |
| Baseline affected | Unapproved Step 6 draft at commit `a7a205e` |
| Release baseline | Not available; project is pre-design and pre-deployment |

## Reason

The project owner wants the current repository milestone to contain only completed and validated lifecycle work through Step 5. The Step 6 artifact was still a draft awaiting human review.

## Impact Analysis

| Artifact | Affected? | Before | After | Risk |
|---|---|---|---|---|
| `06-architecture-design.md` | Yes | Draft present | Removed | Low; no approved design baseline lost |
| Step 5 validation | Metadata only | Ready for Step 6 | Ready, but lifecycle paused after Step 5 | None |
| README/index | Yes | Advertised Step 6 draft | States lifecycle paused after Step 5 | None |
| REQ/NFR/BR/US/AC | No | Approved baseline | Unchanged | None |
| Evidence/skills Step 1–5 | No | Complete | Unchanged | None |
| Code/database/API/UI/tests/deployment | No | Not implemented | Unchanged | None |

## Approved Scope

- Delete only `docs/software-engineering/06-architecture-design.md`.
- Update current-status metadata and navigation to show the lifecycle pause.
- Preserve all Step 1–5 artifacts, IDs, decisions, evidence, and change history.

## Not In Scope

- Reversing the Step 5 design-readiness decision.
- Changing any product requirement or priority.
- Deleting Step 6–15 placeholder directories.
- Implementing architecture, database, API, UI, code, tests, or deployment.

## Approval Gate

| Owner | Required Evidence | Decision | Date |
|---|---|---|---|
| Stieve | Explicit request to remove architecture design and stop at Step 5 | Approved | 1 Juli 2026 |

## Before

An unapproved architecture draft existed and README advertised it as waiting for review.

## After

The architecture draft is absent. Step 5 remains validated and design-ready, but the lifecycle is explicitly paused until the owner asks to continue.

## Verification Needed

1. `docs/software-engineering/06-architecture-design.md` does not exist.
2. Step 1–5 artifacts and traceability counts remain unchanged.
3. README and requirements index state that the lifecycle is paused after Step 5.
4. `git diff --check` passes.

## Rollback and Compatibility

No runtime, database, API, or deployment behavior is affected. If Step 6 is resumed, recreate architecture from the validated Step 5 baseline rather than silently restoring an unreviewed draft.

## Baseline Update Rule

Step 1–5 remain the approved baseline. No design baseline exists after CR-003.

## Routing Decision

No downstream routing. Wait for explicit project-owner instruction before starting Step 6.

## Verification Result

- Step 6 artifact removed.
- Step 1–5 traceability preserved.
- Current-status documentation synchronized.
- Markdown diff check passed.
