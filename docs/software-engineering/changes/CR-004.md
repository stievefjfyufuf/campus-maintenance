# Change Request CR-004

## Request

Fix the remaining final-audit findings before Step 6: invalid project-local Step 1–2 skill metadata, unsynchronized Step 3 priority cells, and a non-reproducible npm lockfile.

## Metadata

| Item | Nilai |
|---|---|
| Requester | Stieve - Project owner/stakeholder proxy |
| Request date | 1 Juli 2026 |
| Type | Documentation correction / build maintenance |
| Urgency | Normal |
| Decision | Approved and applied |
| Approval evidence | User statement: “ok tolong anda perbaiki” |
| Baseline affected | Approved Steps 1–5 at commit `1c0777e` |
| Release baseline | Not available; project is pre-design and pre-deployment |

## Reason

The Step 1–5 business workflow was design-ready, but final repository checks found two invalid reusable-skill manifests, stale priority cells in the specification, and a package lock that failed `npm ci`.

## Impact Analysis

| Artifact | Affected? | Before | After | Risk |
|---|---|---|---|---|
| Skills 01/02 | Yes | No YAML frontmatter; validator failed | Valid frontmatter and UI metadata | Low |
| Step 3 specification | Metadata only | Priority column was `TBD` | MoSCoW copied from approved Step 4 | Low; wording/status/AC unchanged |
| Step 4 prioritization | No | Source of priority decisions | Unchanged | None |
| `package-lock.json` | Verified | `npm ci` previously reported a missing dependency | Lock regeneration produced no content diff; clean `npm ci` now passes | None; package manifest unchanged |
| REQ/NFR/BR/US/AC | No semantic change | Approved baseline | IDs and wording preserved | None |
| Step 6 | No | Not started | Not started | None |

## Approved Scope

- Add valid YAML frontmatter and `agents/openai.yaml` to skills 01 and 02.
- Copy only the approved Must/Should value for REQ-001–REQ-024 from Step 4 into Step 3.
- Regenerate `package-lock.json` without changing `package.json`.
- Run all five skill validators, traceability checks, `npm ci`, lint, and build.

## Not In Scope

- Changing requirement wording, status, AC, scores, release boundary, or stakeholder decisions.
- Starting Step 6.
- Fixing generated Cloudflare type warnings unless they become errors.
- Upgrading dependency versions beyond resolution required by the existing manifest.

## Approval Gate

| Owner | Required Evidence | Decision | Date |
|---|---|---|---|
| Stieve | Explicit request to fix all reported findings | Approved | 1 Juli 2026 |

## Before

Skills 01/02 failed official validation, Step 3 displayed `TBD` after Step 4 had approved priorities, and `npm ci` was not reproducible.

## After

All five project-local skills validate, Step 3 and Step 4 priority values match, and clean dependency installation succeeds.

## Verification Needed

1. Official validator passes skills 01–05.
2. All 24 Step 3 priorities equal their Step 4 MoSCoW decisions.
3. Traceability counts remain 24 REQ, 12 NFR, 14 BR, 16 US, and 48 AC.
4. `npm ci`, lint, build, and `git diff --check` pass.
5. Step 6 remains absent.

## Rollback and Compatibility

No application behavior or API/data compatibility changes. Rollback restores skill metadata, priority display cells, and the previous lockfile; it would also restore the known validation/install failures.

## Baseline Update Rule

The Step 1–5 requirement baseline remains approved. CR-004 updates metadata and build reproducibility only.

## Routing Decision

Route to project-local skill maintenance, Step 3 metadata synchronization, and dependency lock maintenance. Do not route to Step 6.

## Verification Result

| Check | Result |
|---|---|
| Skills 01–05 official validator | Pass: all five report `Skill is valid!` |
| Step 3 vs Step 4 priority | Pass: 24/24 match, 0 mismatch |
| Traceability | Pass: 24 REQ, 12 NFR, 14 BR, 16 US, 48 AC |
| Clean dependency install | Pass: `npm ci` installed 186 packages |
| Lint | Pass: 0 errors; 2 warnings in generated Cloudflare types |
| Production build | Pass |
| Step 6 absence | Pass |
| Markdown whitespace | Pass: `git diff --check` |
