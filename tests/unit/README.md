# Unit Tests

Unit dan regression tests berada di:

- `../domain.test.ts` untuk validasi laporan, ID, hari kerja, dan aturan transisi status.
- `../dashboard.test.ts` untuk izin peran dan normalisasi metrik dashboard.

Jalankan seluruh unit test dengan `npm.cmd test`.

